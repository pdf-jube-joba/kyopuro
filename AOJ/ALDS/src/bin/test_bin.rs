fn main() {
}

fn t(a: &mut[usize], b: &mut[usize]) {

}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn test() {
        let mut a = vec![1,2,3];
        // t(&mut a[0..1], &mut a[1..2])
    }
}