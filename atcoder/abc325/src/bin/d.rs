fn main() {
    proconio::input! {
        n: usize,
        td: [(usize, usize); n],
    }
    println!("{}", max_count(td));
}

fn max_count(td: Vec<(usize, usize)>) -> usize {
    let mut count = 0;
    todo!();
    count
}
